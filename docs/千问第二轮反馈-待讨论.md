# 千问第二轮反馈 - 待讨论问题

> 日期：2026-06-12
> 状态：P0 已修复，P1/P2 待讨论

---

## 一、已修复的 P0 问题

| 问题 | 状态 |
|------|------|
| 全局 QFrame 嵌套边框 | ✅ 改为 class 选择器 |
| Primary 按钮红色 | ✅ 改为 C_ACCENT 蓝色 |
| ComboBox 箭头消失 | ✅ 添加 down-arrow 样式 |
| 缺少 :pressed 状态 | ✅ 添加 pressed 伪状态 |
| 缺少初始窗口尺寸 | ✅ 添加 resize(1060, 720) |
| 状态栏缺引擎名称 | ✅ 添加 "SenseVoice + CAM++ + ct-punc" |
| 日志标题错误 | ✅ 改为 "运行日志" |
| 缺少清除日志按钮 | ✅ 添加清除按钮 |
| 格式标签文本不一致 | ✅ 改为 "输出格式" |

---

## 二、需要讨论的 P1 问题

### 2.1 设置页功能缺失

**问题**：新版设置页缺少以下功能：
- 音频设备 section（VB-Cable 开关）
- 通知开关
- AI 增强 section 缺少 7 个控件（厂商/模型/接入模式/本地LLM/自动摘要/纠错）
- 模型管理缺少详情和下载按钮
- 保存配置缺少多个 key
- restore_config 不完整

**讨论点**：
1. 这些功能是否需要在本次迁移中全部实现？
2. 如果部分实现，优先级如何排序？
3. AI 增强的厂商/模型选择功能是否需要保留？

### 2.2 AddVoiceDialog 录音功能

**问题**：新版仅使用 QInputDialog 获取姓名，缺少录音功能。

**讨论点**：
1. 录音功能是否需要在本次迁移中实现？
2. 如果实现，录音模块需要与 UnifiedRecorder 集成，复杂度较高
3. 是否可以先用"从文件导入"替代"现场录音"？

### 2.3 TranscriptionCompleteDialog

**问题**：转写完成后没有弹窗通知。

**讨论点**：
1. 是否需要实现完整的转写完成弹窗？
2. 还是用 QMessageBox.information 替代即可？

### 2.4 SpeakerDialog 音色库下拉框

**问题**：新版缺少音色库下拉框和匹配建议。

**讨论点**：
1. 音色库下拉框功能是否需要保留？
2. 匹配建议功能是否需要保留？

---

## 三、需要讨论的 P2 问题

### 3.1 UI 样式问题

| 问题 | 讨论点 |
|------|--------|
| 路径输入框固定宽度 | 是否改为弹性宽度？ |
| 保存按钮对齐 | 左对齐还是居中？ |
| 工具栏分隔线 | 是否需要添加？ |
| recording_bar 间距 | 是否需要调整？ |

### 3.2 功能缺失

| 问题 | 讨论点 |
|------|--------|
| DWM 圆角 | Win11 适配是否必要？ |
| 日志过滤 | USER_FRIENDLY_KEYWORDS 是否启用？ |

---

## 四、建议的下一步

### 优先级 1（必须修复）
1. 设置页 AI 增强 section 补全
2. TranscriptionCompleteDialog 实现

### 优先级 2（应该修复）
3. SpeakerDialog 音色库下拉框
4. 录音后询问转写（已实现）
5. 文件变更监听（已实现）

### 优先级 3（可以延后）
6. AddVoiceDialog 录音功能
7. 所有 P2 样式问题

---

## 五、问题清单（供千问参考）

请千问确认以下问题的处理方式：

1. **设置页 AI 增强**：是否需要保留厂商/模型选择？还是简化为只保留 API Key + 开关？
2. **AddVoiceDialog**：录音功能是否必要？还是用"从文件导入"替代？
3. **TranscriptionCompleteDialog**：用完整弹窗还是简单 QMessageBox？
4. **SpeakerDialog 下拉框**：是否需要音色库选择功能？
5. **DWM 圆角**：已实现，是否保留？
6. **日志过滤**：是否启用 USER_FRIENDLY_KEYWORDS？
