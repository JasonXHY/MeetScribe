# MeetScribe Dependencies
# Core
customtkinter>=5.2.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# FunASR (ASR + Speaker Diarization + Punctuation)
funasr>=1.0.0
# Models auto-downloaded from ModelScope

# System Audio Recording (WASAPI Loopback)
pyaudiowpatch>=0.2.12

# AI Service (OpenAI-compatible API)
openai>=1.0.0

# Optional: FFmpeg for audio format conversion
# Install separately: https://ffmpeg.org/download.html
